# Qwen3 微调项目依赖包

# 深度学习框架
torch>=2.0.0
transformers>=4.50.0

# 模型微调相关
peft>=0.6.0
accelerate

# 数据处理
datasets==3.2.0
pandas

# 模型下载和管理
modelscope==1.22.0

# Web界面
gradio>=4.0.0

# 其他工具
numpy>=1.24.0
regex>=2023.8.8
tqdm>=4.65.0
addict
swanlab

# 可选：用于更好的性能
# bitsandbytes>=0.41.0  # 4bit/8bit量化（可选）
# flash-attn>=2.3.0     # Flash Attention（可选，需要特定GPU）
# deepspeed>=0.12.0     # DeepSpeed分布式训练（可选）

# 强化学习与评估阶段
trl==0.7.10
rich>=13.0.0
openai>=1.0.0,<1.50.0
protobuf>=3.20,<5
sentencepiece>=0.1.99

